ext {
    libs = [
            main      : [
                    common     : [
                            "com.android.support:appcompat-v7:${versions.sdk}",
                            "com.android.support:support-v4:${versions.sdk}",
                            "com.android.support:design:${versions.sdk}",
                            "com.android.support.constraint:constraint-layout:${versions.constraintLayout}",
                            "com.google.guava:guava:${versions.guava}"
                    ],
                    butterKnife: "com.jakewharton:butterknife:${versions.butterKnife}",
                    storeBox   : "net.orange-box.storebox:storebox-lib:${versions.storeBox}",
                    DBFlow     : [
                            "com.github.Raizlabs.DBFlow:dbflow-core:${versions.DBFlow}",
                            "com.github.Raizlabs.DBFlow:dbflow:${versions.DBFlow}"
                    ]
            ],
            annotation: [
                    butterKnife: "com.jakewharton:butterknife-compiler:${versions.butterKnife}",
                    DBFlow     : "com.github.Raizlabs.DBFlow:dbflow-processor:${versions.DBFlow}",
            ],
            test      : [

            ]
    ]
}