apply plugin: 'kotlin-kapt'

android {
    defaultConfig {
        applicationId "alvin.ui.merge"
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation libs.main.butterKnife

    kapt libs.annotation.butterKnife
}
