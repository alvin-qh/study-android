
android {
    defaultConfig {
        applicationId "alvin.butterknife"
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile libs.main.common
    compile libs.main.butterKnife

    annotationProcessor libs.annotation.butterKnife
}
